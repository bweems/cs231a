The random forest code that they use actually has compilation errors that I had to fix.  I did not fix them in the training function, only in the prediction function, so the training function will not work
